package Day32_ConstructorOverloading;

public class CallRules {

    //1. Constructor can not be called by its name.( this() needs to be used)
    //2. Constructor call can only happen in another constructor
    //3. Constructor call must be the first statement in body
    //4. One constructor can only call one constructor
    //5. Constructor cannot call/contain itself





    //this.  : used to call instances & instance methods(rare cases)
    //this() : used to call constructors
}
